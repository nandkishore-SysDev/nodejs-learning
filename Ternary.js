const prompt = require("prompt-sync")({sigint:true});

// let marks = prompt("Enter your marks: ");

// let result = (marks >= 40) ? 'passed' : 'failed';

// console.log(`You ${result} the exam.`);


// let age = 19;
// let result1;

// if (age >= 18) {
//     result1 = "eligible to vote";
// } else {
//     result1 = "not eligible to vote";
// }

// result2 = (age >= 18) ? "eligible to vote" : "not eligible to vote";

// console.log(`Result is ${result2}`);

let marks1 = prompt("Enter your marks: ");

let result3 = (marks1 >= 40) ? 'passed' : 'failed';

console.log(`Result is ${result3}`);